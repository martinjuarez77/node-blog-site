<%- include('partials/header') -%>

 <h1>Home page</h1>
<br>
<%=homeContent%>
<br>
<% postsList.forEach(function(post){
  var textoPost = post.text;
  if (textoPost.length > 10){
    textoPost = textoPost.substr(0, 7) + "...";
  }
  %>
  <h1><%=post.title%></h1>
  <%=textoPost%> <%=textoPost%>
  <% if (post.text.length > 10 ){ %>
    <a href='/posts/<%=post.title%>'>Ver mas</a>
  <% } %>
  <hr>
  <%
}) %>
<%- include('partials/footer') -%>
